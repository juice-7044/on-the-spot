'use client'

import { useEffect, useState } from 'react'

const statuses = ['New', 'Under Review', 'Accepted', 'Agreement Sent', 'Signed', 'Hired', 'Rejected']
type Application = { id: string; full_name: string; email: string; phone: string; position: string; experience_years: number; status: string; created_at: string; notes?: string | null; resume_url?: string | null; message?: string | null }

export default function AdminDashboard() { const [applications, setApplications] = useState<Application[]>([]); const [filter, setFilter] = useState('All'); const [error, setError] = useState(''); const [loading, setLoading] = useState(true)
  async function load() { const response = await fetch('/api/admin/applications'); const result = await response.json(); if (!response.ok) { setError(result.error || 'Unable to load applications.'); setLoading(false); return }; setApplications(result.applications); setLoading(false) }
  useEffect(() => { load() }, [])
  async function update(id: string, status: string, notes?: string | null) { const response = await fetch('/api/admin/applications', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id, status, notes }) }); if (!response.ok) { setError('Unable to update application.'); return }; setApplications((items) => items.map((item) => item.id === id ? { ...item, status, notes } : item)) }
  async function logout() { await fetch('/api/admin/logout', { method: 'POST' }); window.location.href = '/admin/login' }
  const visible = filter === 'All' ? applications : applications.filter((item) => item.status === filter)
  return <main className="min-h-screen bg-background px-6 py-10 md:px-10"><div className="mx-auto max-w-7xl"><header className="flex flex-wrap items-end justify-between gap-5 border-b border-border pb-6"><div><p className="font-mono text-xs uppercase tracking-[0.2em] text-primary">Private operations</p><h1 className="mt-2 text-4xl font-black uppercase">Candidate pipeline</h1><p className="mt-2 text-muted-foreground">{applications.length} total applications</p></div><button onClick={logout} className="border border-border px-4 py-2 text-sm font-bold uppercase hover:border-primary">Sign out</button></header><div className="mt-8 flex flex-wrap gap-2">{['All', ...statuses].map((status) => <button key={status} onClick={() => setFilter(status)} className={`border px-3 py-2 text-xs font-bold uppercase ${filter === status ? 'border-primary bg-primary text-primary-foreground' : 'border-border hover:border-primary'}`}>{status}</button>)}</div>{error && <p role="alert" className="mt-6 text-sm text-destructive">{error}</p>}{loading ? <p className="mt-10 text-muted-foreground">Loading applications…</p> : <div className="mt-8 grid gap-4">{visible.map((application) => <article key={application.id} className="border border-border bg-card p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-bold">{application.full_name}</h2><p className="mt-1 text-sm text-muted-foreground">{application.position} · {application.experience_years} years · {new Date(application.created_at).toLocaleDateString()}</p><p className="mt-2 text-sm">{application.email} · {application.phone}</p></div><select value={application.status} onChange={(event) => update(application.id, event.target.value, application.notes)} className="border border-input bg-background px-3 py-2 text-sm">{statuses.map((status) => <option key={status}>{status}</option>)}</select></div><details className="mt-5 border-t border-border pt-4"><summary className="cursor-pointer text-sm font-bold uppercase">View details</summary><div className="mt-4 grid gap-3 text-sm text-muted-foreground"><p>{application.message || 'No additional message.'}</p><div className="flex flex-wrap gap-3"><button type="button" onClick={async () => { const response = await fetch('/api/admin/agreements', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ applicationId: application.id }) }); const result = await response.json(); if (!response.ok) setError(result.error || 'Unable to send agreement.'); else setError(`Agreement sent to ${application.email}.`) }} className="border border-primary px-3 py-2 text-xs font-bold uppercase text-primary hover:bg-primary hover:text-primary-foreground">Send agreement</button></div><label className="text-foreground">Internal notes<textarea defaultValue={application.notes || ''} onBlur={(event) => update(application.id, application.status, event.target.value)} className="mt-2 min-h-24 w-full border border-input bg-background p-3" /></label>{application.resume_url && <p>Resume stored securely in Supabase Storage.</p>}</div></details></article>)}{visible.length === 0 && <p className="border border-dashed border-border p-10 text-center text-muted-foreground">No applications in this status.</p>}</div>}</div></main>
}
